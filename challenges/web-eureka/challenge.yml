name: "Eurêka !"
author: "M€dus@"
category: Web                    # Web, Code Review, Crypto, Forensic, Reverse, OSINT, Misc

description_image: files/challenge.png   # OBLIGATOIRE pour l'affichage dans la description

description: |
  Eurêka ! Un secret flotte sous la surface d’un service web. Lis la surface, provoque la poussée, et l’œil attentif sera récompensé.

value: 300                       # Voir document de Camille
type: dynamic                    # ou: container, firstblood
state : visible                  # Permet de voir l'état dans le panel admin

extra:
  initial: 300                   # POur gerer le dynamisme du scoring
  decay: 13                      # facteur de decroissance
  minimum: 100                   # Minimum

tags:
  - MOYEN                        # OBLIGATOIRE en premier (voir tableau)
  - Web

flags:
  - flag: "NHK26{ARCHIMEDE_te_donne_le_m@st€r_c00kie}"
    type: static

type: container
image: registry.gitlab.com/ex-ex1/ex-ex/web-eureka:latest
port: 5000 # Port exposé dans votre Dockerfile
protocol: web                    # ou: tcp
