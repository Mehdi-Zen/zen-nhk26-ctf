server {
    listen 5000;
    server_name _;

    root /usr/share/nginx/html;
    index index.html;

    # SPA fallback for Vite/React routing
    location / {
        try_files $uri $uri/ /index.html;
    }
}
